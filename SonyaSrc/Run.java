
public class Run {
	/**
	public void findResponder(Request request){
		int maxMatch = 0;
		String maxMatchID;
	for (int i = 0; i<sizeOfDatabase; i++){
		if (databaseEntry[i].getResponder == true && databaseEntry[i].getOnline == true){
			if (calculate(responder[i]) > maxMatch){
				maxMatch = calculate(responder[i]);
				maxMatchID = responder[i].getID();
			}
		}
	}**/
	// Loop through every value in database which is a responder and run score
	// responder on them
	// Based on timescale, place the request into the responder's queue

}
}
