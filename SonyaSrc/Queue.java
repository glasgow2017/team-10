

public class Queue {
	/**int[] array;
	int n = 0;
	public void createQueue(int responderID){
		for (int i = 0; i<sizeOfDatabase; i++){
			if (responderId.getvalue[i] == responderID){
				array[n] = i;
			}
		}
	}**/
// loop through all requests for specific responder id and sort by timescale
	// within that loop through all requests and sort by time added so that those added first are higher in the list
}
